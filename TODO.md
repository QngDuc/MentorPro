# TODO
- [x] Create edit plan to replace Deepseek logo with Metor logo across the app (login + chat screens).

- [ ] Update `frontend/app/page.tsx` to use `MetorLogo` instead of `DeepseekLogo`.
- [ ] Update `frontend/app/chat/page.tsx` to use `MetorLogo` (including compact usage).
- [ ] Update CSS selectors in `frontend/app/globals.css` from `.deepseek-logo*` to `.metor-logo*` where needed (login-card/chat-start sizing).
- [ ] Search for any remaining `DeepseekLogo` / `deepseek-logo` references and update.
- [ ] Run frontend lint/build (or dev start) to ensure TypeScript compiles.

